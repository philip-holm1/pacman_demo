# Research — Green Star Skins

## Decisions

- Rendering library: NEEDS CLARIFICATION — confirm pygame and sprite pipeline.
- Asset override mechanism: NEEDS CLARIFICATION — determine loader hook to map skin paths.
- Preference persistence: NEEDS CLARIFICATION — decide JSON file vs `config.py`.
- Performance validation: Use simple frame timing check; ensure no frame drop.

## Tasks

- Research rendering stack in `src/pacman` (game loop, sprite loading).
- Identify asset loading functions and propose override mapping for `assets/sprites/skins/green_star/`.
- Evaluate persistence mechanism: extend `config.py` or add `data/settings.json`.
- Define frightened ghost visual variant within comet-tail theme (contrast/outline).

## Consolidated Findings (to be updated)

- Decision: [TBD]
- Rationale: [TBD]
- Alternatives considered: [TBD]
