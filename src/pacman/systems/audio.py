from .game_state import GameState

# Simple stub; real implementation would load and play pygame.mixer sounds.

def play_powerup_pickup(gs: GameState, powerup_type: str) -> None:
    # Could log or append a feedback entry for tests; choose not to to keep scoring tests isolated.
    pass
